// import { expect } from "chai";
// import { showErrors } from "./error-reporting";
// import { compile } from "./compiler";

describe("compile()", () => {
  it("Test!.", () => {
    // compile("1");
    // compile("1 * 2");
    // compile("10 + 1");
    // compile("10 + \"diko\"");
    // compile("-1 + 2 * 10 + \"diko\"");
    // compile("0 ? 1 : 2");
    // compile("\"diko\" =~ /[a-z]/ ? 1 : 2");
    // compile("? + 1 - ?");
    // compile("name from ?; @name");
  });
});
